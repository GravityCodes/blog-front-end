<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Noto+Sans:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
		<title>Johan M. Mesa</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style>
	:root {
		--background-color:#1a1a1a;
		--foreground-color:#ffffff;
		--accent-color-1: #d2d2d2;
		--accent-color-2: #464646;
		--accent-color-3: #131313;
		
		--primary-color: #5eacf0;
		--primary-accent-color: #335a7d;
		--error-color: #f05e5e;
		--error-accent-color: #b25353 ;
	}

	.msg-icon{
		color: white;
	}

	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
		font-family: "Noto Sans", sans-serif;
		font-optical-sizing: auto;
		font-weight: <weight>;
		font-style: normal;
		font-variation-settings:
			"wdth" 100;
		background-color: var(--background-color);
		min-height: 100vh;
		display: flex;
		color: var(--foreground-color);
		flex-direction:column;
	}

	@media (prefers-color-scheme: light) {
		:root {
				--background-color:#ffffff;
				--foreground-color:#1a1a1a;
				--accent-color-1: #ededed;
				--accent-color-2: #dfdfdf;
				--accent-color-3: #d2d2d2;
		}
		img {
			filter: invert(1);
		}
		.msg-icon{
			color: black;
		}

	}

</style>
